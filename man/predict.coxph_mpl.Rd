% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods.R
\name{predict.coxph_mpl}
\alias{predict.coxph_mpl}
\alias{plot.predict.coxph_mpl}
\title{Predictions for a Cox Model Fit via MPL}
\usage{
\method{predict}{coxph_mpl}(
  object,
  se = "M2QM2",
  type = "risk",
  i = NULL,
  time = NULL,
  upper.quantile = 0.95,
  ...
)

\method{plot}{predict.coxph_mpl}(x, ...)
}
\arguments{
\item{object}{A fitted model of class \code{"coxph_mpl"}.}

\item{se}{Inference method for confidence intervals. One of \code{"H"},
\code{"M2QM2"}, or \code{"M2HM2"}. Default \code{"M2QM2"}.}

\item{type}{Prediction type: \code{"risk"} for instantaneous risk or
\code{"survival"} for survival probability. Default \code{"risk"}.}

\item{i}{Optional integer index of the observation whose covariates are used.
Defaults to mean covariates.}

\item{time}{Optional numeric vector of times at which to predict. Defaults to
1000 equally spaced times over the outcome range.}

\item{upper.quantile}{Quantile of the response used to bound the x-axis when
plotting. Default \code{0.95}.}

\item{...}{Additional plotting parameters.}

\item{x}{An object of class \code{"predict.coxph_mpl"}.}
}
\value{
A data frame of class \code{"predict.coxph_mpl"} with columns
  \code{time}, \code{risk} or \code{survival}, \code{se}, \code{low}, and
  \code{high}.
}
\description{
Compute predicted instantaneous risk or survival probabilities for a fitted
\code{coxph_mpl} model.
}
\details{
Predictions incorporate the baseline hazard or cumulative baseline
  hazard, giving absolute (not relative) risk and survival estimates.
  Standard errors and confidence intervals are computed via the delta method
  and truncated to the parameter range.
}
\examples{
\dontrun{
data(lung)
fit_mpl <- coxph_mpl(Surv(time, status == 2) ~ age + sex + ph.karno + wt.loss,
                     data = lung)
plot(predict(fit_mpl))
}
}
\seealso{
[coxph_mpl()], [coxph_mpl.control()], [residuals.coxph_mpl()],
  [summary.coxph_mpl()]
}
