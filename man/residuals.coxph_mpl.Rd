% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/residuals.R
\name{residuals.coxph_mpl}
\alias{residuals.coxph_mpl}
\alias{plot.residuals.coxph_mpl}
\title{Residuals for a Cox Model Fit via MPL}
\usage{
\method{residuals}{coxph_mpl}(object, ...)

\method{plot}{residuals.coxph_mpl}(x, ask = TRUE, which = 1:2, upper.quantile = 0.95, ...)
}
\arguments{
\item{object}{A fitted model of class \code{"coxph_mpl"}.}

\item{...}{Additional plotting parameters.}

\item{x}{An object of class \code{"residuals.coxph_mpl"}.}

\item{ask}{Logical; whether to prompt before each plot. Default \code{TRUE}.}

\item{which}{Integer vector selecting residual plots (\code{1:2}).}

\item{upper.quantile}{Quantile used to bound the y-axis for Cox-Snell
residuals when \code{which == 3}. Default \code{0.95}.}
}
\value{
A data frame of class \code{"residuals.coxph_mpl"} with columns
  \code{time1}, \code{time2}, \code{censoring}, \code{coxsnell}, and
  \code{martingale}.
}
\description{
Compute martingale and Cox-Snell residuals for a \code{coxph_mpl} model. The
returned object has a plot method.
}
\examples{
\dontrun{
data(lung, package = "survival")
fit_mpl <- coxph_mpl(Surv(time, status == 2) ~ age + sex + ph.karno + wt.loss,
                     data = lung)
par(mfrow = c(1, 2))
plot(residuals(fit_mpl), which = 1:2, ask = FALSE)
}
}
\references{
Farrington (2000), Collett (2003), Moeschberger (2003).
}
\seealso{
[coxph_mpl()], [predict.coxph_mpl()], [summary.coxph_mpl()]
}
