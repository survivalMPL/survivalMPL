% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/coef.R
\name{coef.summary.coxph_mpl}
\alias{coef.summary.coxph_mpl}
\alias{coef.coxph_mpl}
\title{Extract Coefficients from \code{coxph_mpl} Fits}
\usage{
\method{coef}{summary.coxph_mpl}(object, parameters = "Beta", ...)

\method{coef}{coxph_mpl}(object, parameters = "Beta", ...)
}
\arguments{
\item{object}{An object of class \code{"coxph_mpl"} or
\code{"summary.coxph_mpl"}.}

\item{parameters}{Parameter set of interest: \code{"Beta"} for regression
coefficients or \code{"Theta"} for baseline hazard parameters. Default
\code{"Beta"}.}

\item{...}{Additional arguments passed to methods.}
}
\value{
A vector of coefficients or a matrix with estimates, standard errors,
  z-statistics, and p-values.
}
\description{
Extract Coefficients from \code{coxph_mpl} Fits
}
\details{
For \code{summary.coxph_mpl} inputs with \code{parameters == "Theta"},
  only baseline hazard estimates exceeding \code{min.theta} are reported.
}
\examples{
\dontrun{
data(lung, package = "survival")
fit_mpl <- coxph_mpl(Surv(time, status == 2) ~ age + sex + ph.karno + wt.loss,
                     data = lung)
coef(fit_mpl)
coef(fit_mpl, parameters = "Theta")
coef(summary(fit_mpl))
}
}
\seealso{
[coxph_mpl()], [summary.coxph_mpl()]
}
