% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods.R
\name{print.summary.coxph_mpl}
\alias{print.summary.coxph_mpl}
\alias{summary.coxph_mpl}
\title{Print Method for \code{summary.coxph_mpl}}
\usage{
\method{print}{summary.coxph_mpl}(x, se = "M2QM2", ...)

\method{summary}{coxph_mpl}(object, se = "M2QM2", full = FALSE, ...)
}
\arguments{
\item{x}{An object of class \code{"summary.coxph_mpl"}.}

\item{se}{Inference method. One of \code{"H"}, \code{"M2QM2"}, or
\code{"M2HM2"}. Default is \code{"M2QM2"}.}

\item{...}{Additional arguments passed to methods.}

\item{object}{A fitted model of class \code{"coxph_mpl"}.}

\item{full}{Logical; if \code{TRUE}, include inference for baseline hazard
parameters. Default \code{FALSE}.}
}
\value{
Invisibly returns \code{x}.

An object of class \code{"summary.coxph_mpl"} with components:
  \item{Beta}{Matrix of regression estimates, standard errors, z-statistics,
  and p-values.}
  \item{Theta}{Baseline hazard estimates (or, if \code{full = TRUE}, a matrix
  of estimates with standard errors, z-statistics, and p-values).}
  \item{inf}{List with convergence details, penalised likelihood value, and
  control settings.}
}
\description{
Extracts additional information for a fitted model and returns an object
suitable for printing. Baseline hazard parameters smaller than
\code{min.theta} are omitted unless \code{full = TRUE}.
}
\examples{
\dontrun{
data(lung)
fit_mpl <- coxph_mpl(Surv(time, status == 2) ~ age + sex + ph.karno + wt.loss,
                     data = lung)
summary(fit_mpl, full = TRUE)
summary(fit_mpl, se = "M2HM2")
}
}
\seealso{
[summary.coxph_mpl()], [coxph_mpl()], [coxph_mpl.control()]

[coxph_mpl()], [coxph_mpl.control()], [plot.coxph_mpl()]
}
