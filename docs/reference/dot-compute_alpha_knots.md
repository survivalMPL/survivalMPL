# Compute the Alpha knot sequence from event times. Called by every basis-specific knots_fn.

Compute the Alpha knot sequence from event times. Called by every
basis-specific knots_fn.

## Usage

``` r
.compute_alpha_knots(control, events)
```
