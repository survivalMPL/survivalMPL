# Compute the penalty order for a basis. Replaces `penalty.order_mpl()`.

Compute the penalty order for a basis. Replaces `penalty.order_mpl()`.

## Usage

``` r
compute_penalty_order(basis_name, p, order)
```
