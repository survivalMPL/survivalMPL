# Compute the density or cumulative basis matrix. Replaces `basis_mpl()`.

Compute the density or cumulative basis matrix. Replaces `basis_mpl()`.

## Usage

``` r
compute_basis_matrix(x, knots, basis_name, order, which = 1)
```
