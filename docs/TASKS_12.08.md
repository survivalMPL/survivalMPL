# NA

General: did we have all those plots in original survivalMPL package
that is currently on CRAN? General: Check that we very consistent in our
articles. Use the same terms in the same context. follow the same logic
in code example. So it should look like a flow.

General: comparing coxph and coxph_mpl article should be Comparing
coxph() and coxph_mpl() index page: installation and right censoring
example with reference to article to check more about getting started
Right censoring: “Baseline hazard and survival” there is a legend but I
cant see anything from that legend on the plot: no grey/red dots, no
dashed grey lines. But there is a colour changing in uniform basis (not
clear why, what is the pattern) Right censoring: Residuals: dots overlap
the legend Right censoring: Estimate the baseline hazard function plot
legend overlaps with the plot. dashed grey line is invisible Right
censoring: Don’t include left truncation, we have article about it
(could be mentioned in “Next steps” section)

General: remove long dashes from all articles Left censoring: Example —
pseudo-melanoma data to Example: pseudo-melanoma data and keep this
style for consistency. Left censoring: plot(fit_left) Msplines barely
readeble everything happenin in 1 year with long tail up to year 4 Left
censoring: Shall we add the plot to the Example — bcos2? Only if plots
are avail via plot()

Write a short article on left truncation based on Jun’s text
(left_truncation_profile_likelihood (2).pdf in dev/experiments). Use
that text just to know the idea and correct wording behind the left
trucation
